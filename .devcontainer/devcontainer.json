{
  "name": "C#",
  "build": { "dockerfile": "Dockerfile" },
  // The Dockerfile runs as root; no separate user is created.
  "remoteUser": "root",
  // Load local secrets (GitHub token, etc.) from .devcontainer/.env.
  // Copy .env.example to .env first; the real .env is gitignored.
  "runArgs": ["--env-file", "${localWorkspaceFolder}/.devcontainer/.env"],
  "customizations": {
    "vscode": {
      "extensions": ["ms-dotnettools.csharp", "ms-dotnettools.csdevkit", "anthropic.claude-code"]
    }
  }
}
